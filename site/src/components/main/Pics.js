import React from 'react'

function Pics({ Scrolled }) {
    console.log(Scrolled);
    return (
        <section id='pics' className='myScroll'>
            Pics
        </section>
    );
}

export default Pics
